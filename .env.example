# ========================
# 🔓 Public variables (accessible in browser)
# ========================
# Base URL of your backend API (local/dev/prod)
NEXT_PUBLIC_API_BASE=<your-api-url>

# ========================
# 🔒 Server-only secrets (never exposed to client)
# ========================
# Example secret key for JWT or encryption
API_SECRET=<your-secret-key>

# Email server settings (for notifications, if used)
EMAIL_SERVER=<smtp-server>
EMAIL_PORT=587
EMAIL_USER=<your-email@example.com>
EMAIL_PASS=<your-email-password-or-app-password>

# Database connection (if used in backend)
DATABASE_URL=<postgres-url-or-other-db-connection>
